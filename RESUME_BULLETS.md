### ATS-Friendly Resume Bullet Points

- **Architected and deployed a production-grade Multimodal RAG application**, orchestrating parallel ingestion of PDFs, audio transcripts (Whisper API), and video frame descriptions (OpenCV + Vision LLMs) to build a highly contextualized vector knowledge base.
- **Engineered a precision retrieval pipeline combining dense vector embeddings (ChromaDB) and sparse keyword matching (BM25)**, applying cross-encoder re-ranking to boost retrieval accuracy and enforce zero-hallucination, strictly cited LLM responses.
- **Designed a scalable, multi-LLM generation layer (OpenAI, Gemini, Groq, Claude) with an async architecture**, implementing intelligent batch processing and Streamlit resource caching to prevent latency spikes while serving an interactive custom UI.
